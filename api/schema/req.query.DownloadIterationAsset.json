{
    "type": "object",
    "additionalProperties": false,
    "required": [
        "type",
        "token"
    ],
    "properties": {
        "type": {
            "type": "string",
            "enum": [
                "model",
                "tfrecord",
                "checkpoint"
            ],
            "description": "Asset Type to upload"
        },
        "token": {
            "type": "string",
            "description": "Authentication Token"
        }
    }
}
